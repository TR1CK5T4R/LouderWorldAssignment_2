# ─────────────────────────────────────────────
# chat-ui Frontend Environment Variables
# ─────────────────────────────────────────────
# All variables MUST be prefixed with VITE_ to be exposed to the browser.
# Restart `npm run dev` after changing any value here.

# ─────────────────────────────────────────────
# Backend API
# ─────────────────────────────────────────────

# Base URL of the backend REST API.
# In dev, the Vite proxy forwards /api/* to this address automatically.
# Only set this explicitly if your backend is on a different host/port.
VITE_API_BASE_URL=http://localhost:3000

# ─────────────────────────────────────────────
# Socket.io (Real-time notifications)
# ─────────────────────────────────────────────

# Direct WebSocket connection to the backend.
# Must be set explicitly — do NOT use '/' or the Vite proxy for Socket.io
# because the proxy breaks (EPIPE) every time the backend restarts.
#
# Local development:
VITE_SOCKET_URL=http://localhost:3000
#
# Production (uncomment and replace with your deployed backend URL):
# VITE_SOCKET_URL=https://your-api-domain.com

# ─────────────────────────────────────────────
# App Meta
# ─────────────────────────────────────────────

# Application display name (used in <title> and header)
VITE_APP_NAME=LouderWorld

# App version (optional, shown in footer/about)
VITE_APP_VERSION=1.0.0

# ─────────────────────────────────────────────
# Feature Flags
# ─────────────────────────────────────────────

# Set to "true" to bypass login and use a demo user automatically.
# Useful for UI development when the backend auth route is not ready.
VITE_DEV_BYPASS_AUTH=false
